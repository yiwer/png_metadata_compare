{
  "$comment": "Optional comparator config. Rename to compare-config.json next to the binary (or in ./config/) to activate. Falls back to bundled defaults if absent.",

  "ignored_fields": [
    "Lines[*].RouteStops[*].Sequence",
    "RenderTime",
    "QRCode",
    "StopId",
    "Lines[*].LinePattern",
    "GroupItems[*].Distance"
  ],

  "equivalence_maps": {
    "Lines[*].RouteStops[*].BuildingType": {
      "Metro": "地铁站",
      "Hospital": "医院",
      "Train": "火车站",
      "Airport": "机场",
      "Tram": "*",
      "Wharf": "*",
      "School": "*",
      "PassengerStation": "*",
      "Bus": "*"
    },
    "Lines[*].ScheduledServiceDescription": {
      "（工作日运营）": "（工作日运行）"
    }
  }
}
